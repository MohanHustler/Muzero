export interface Package {
  name : string;
  courseCount: number;
  studentCount: number;
  batchCount: number;
  sessionHourCount:number;
  contentSize: number;
  customChapter: number;
  quizCount: number;
}